{
  "name": "simple-test-server",
  "version": "1.0.0",
  "description": "Simple test server to verify server environment",
  "main": "simple_server.js",
  "scripts": {
    "start": "node simple_server.js",
    "dev": "nodemon simple_server.js",
    "test": "node test_endpoints.js",
    "build": "node build_deploy.js",
    "deploy": "npm run build && echo Ready for cPanel upload"
  },
  "keywords": ["test", "server", "express"],
  "author": "Test",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=14.0.0"
  }
} 